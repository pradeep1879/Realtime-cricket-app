{
  "name": "cricket-exchange-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "bun run dist/index.js",
    "prisma:generate": "bunx prisma generate",
    "prisma:migrate": "bunx prisma migrate dev",
    "prisma:push": "bunx prisma db push"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.1.1",
    "@prisma/client": "^6.7.0",
    "elysia": "^1.1.25",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^6.7.0",
    "typescript": "^5.8.3"
  }
}
